# Mess Admission Frontend

React/Vite frontend for student mess admission, attendance marking, and admin admission reports.

## Scripts

- `npm run dev` starts the local Vite server.
- `npm run build` creates a production build.
- `npm run lint` checks the React source.
